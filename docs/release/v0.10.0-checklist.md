# ASC v0.10.0 — 릴리스 체크리스트 (minor · 정상 경로가 advanced primitive 없이 닫힌다)

정본 경로는 2단계다: `release.yml` (publish) 이후 `release-finalize.yml` (tag·Release).
finalize 는 이 파일의 `## 실행 기록` 섹션이 `main` 에 있어야 돈다.

## 이 릴리스가 싣는 것

0.9.1 제품 dogfood(2026-09-13, 자연어 → MR 관통)에서 사람이 알아야 했던 일곱 가지를 그 층에서 닫는다.

```text
#89  P1  ACTION_PAYLOAD (required/optional/forbidden) · git.push 본문 거절(exit 2) · Core 는 payloadRequired 입력
         setup 이 local 승인자 후보를 발견 → ASC_LOCAL_IDENTITY_REQUIRED (자동 권한 부여 없음) · gate 가 local 검사
         --as 는 local 승인자 정확히 1명일 때만 생략 · 건네는 명령 shell-quote
     P2  core/operator/blockers.ts (semantic remediation) · cli/remediation.ts 표 하나 · status Blocked:/Next: · CONFLICT stdout
     P3  ContractProposal (CREATED→ISSUED|REVISED|REJECTED|OBSOLETE, adapters/proposals) · asc work issue · setup delegate
         발급 기록 authority/delegatedBy/issuedBy/recordedBy 분리
     P4  asc work reclaim (근거 → 사람이 인수) · terminal-holder audited RELEASED
     P5  exact key · 빈 가지 · IN_PROGRESS_ELSEWHERE · off-target 언급 → UNDECIDABLE · --fresh override 기록
```

Core 에 provider 의미 없음. Guard/F6/셸 interception 없음. 새 opaque 없음. resource identity·multi-binding·GitHub review·canonicalProposal·alias 제거·guard rename 은 **이번 주제 밖** (변경 없음).

## Gate (publish 전)

```text
[x] npm run typecheck / build / test          — 1727 / 1726 pass / 0 fail / 1 skip
[x] npm run release:check                     — 0.10.0 명명·note 섹션·버전 리터럴
[x] npm run pack:all && npm run smoke
[ ] npm run presentation:check                — 비게이트 (미실행)
[x] 3-OS CI green on main                     — #89 · #90
```

## 수동 fallback (워크플로 불능 시에만) — 이번 회차 미사용

```text
[ ] publish @asc-agent/runtime   → verify on the registry
[ ] publish @asc-agent/bootstrap → verify   (bootstrap 이 runtime 을 exact 로 가리킨다 — 순서 고정)
[ ] published smoke → 인수 → finalize
```

## Published acceptance (게시본으로만 · 실 workspace)

0.9.0 A-1~A-12(managed git.push 관통)·0.9.1 P-1~P-6 은 되풀이하지 않는다 — executor·GitLab·binding-identity 경로는
이 회차가 건드리지 않았다. 이 회차가 바꾼 것만, **실제 제품 작업으로** 본다. 사전 상태(identities·override·lock·
binding 수·inbox 수)를 D-0 에 적고 종료 후 비교한다. cleanup 은 acceptance 와 분리한다.

```text
[x] D-0  사전 상태 기록 (macOS 실기계, 실 제품 workspace)   identities local 매핑 있음(0.9.1) · 위임 미결정 · binding 20 · inbox 126
[x] Q-1  asc update 0.9.1 → 0.10.0 · setup 질문   PASS — local 은 이미 매핑돼 있어 ASC_ISSUANCE_DELEGATION_DECISION 만 1회 · `setup delegate --role implementer` → override 기록·재고정 → already_configured
         (관찰: 출력을 `| head -1` 로 자르면 EPIPE 로 재고정 전에 죽는다 — 테스트 harness 문제, 제품 결함 아님)
[x] Q-2  작업 A' (-725 진단 부재, MANUAL)   PASS — work start(NEEDS_DECISION: boundary·criteria 를 agent 가 --boundary/--criteria/--provenance 로 채움) → 위임 발급+CLAIMED 한 명령
         → 구현 (vitest 1492) → publish git.push (--body-file·--as 없이 G-4503 EXECUTED) → gitlab.mr.create (G-9066, MR !878) → finish.
         사람이 알아야 한 내부 primitive 0 · T 멈춤 0 · 발급 기록 issuedBy=run:… authority=delegated delegatedBy=policy:issuanceDelegation (colosair) recordedBy=run
[ ] Q-3  작업 B' (AUTO)   NOT TAKEN — 동등한 미착수 FE 작업 없음 (남은 항목은 blocked/설계/타인 배정)
[ ] Q-4  비위임 proposal → work issue   **FAIL** — proposal 저장·`asc work issue S-20260914-03` 한 줄·work status 표시까지 PASS, 그러나 `work issue` 가 `CANONICAL_UNAVAILABLE` 로 거절
         (SessionRuntime 에 scm 미전달 — session issue 와 다른 조립). 0.10.1 #91 에서 수정. 제안은 REJECTED 로 닫음
[x] Q-5  status LOCK_DRIFT   PASS — (실 drift: delegate 출력 절단으로 생김) Blocked: `a person decides: asc profile resolve --write` · Next 동일 · work start stderr 동일. approval 제거 검사는 미실행
[x] Q-6  WORK_STATE   부분 PASS — 빈 가지: 병합으로 읽지 않음(ACTIONABLE, 위임이라 S-02 발급됨 → 산출물 없이 finish) · 키 뒤바뀜(-641): UNDECIDABLE(implementation-evidence) 근거 정확 · --fresh: history 에 work_state_overridden
         **결함**: -649(병합 완료, worktree 미커밋 0) 가 UNDECIDABLE — 작업 항목의 패키지 상대 경로와 commit 의 뿌리 경로가 접두 일치 안 됨. 0.10.1 #91 에서 꼬리 일치 추가
[x] R-2  recovery   PASS — stderr·stdout 모두 소유권 없음 + `asc work reclaim S-20260914-04` · status Blocked/Next 동일 · reclaim 이 holder·since·last progress 를 보이고 rebind · finish
[ ] Q-7  Windows 실기계   NOT TAKEN — 이 회차에 Windows 기계 없음
[x] D-10 외부 잔여물: MR !878 · branch fix/-725-realtime-diagnostics (의도) — 그 외 0. 위임은 implementer 로 유지(사람 결정). 세션 S-01(DONE·MR) · S-02·S-04 (관측용 DONE) · 제안 S-03 REJECTED
```

Q-2 가 이 회차의 본체다. 합격 = Q-2(·Q-3) 에서 사람이 알아야 한 내부 primitive 0 · T 멈춤 0 · status 가 매 막힘에 이유+다음 행동 1개.
Q-2/Q-3 는 **새 Claude 세션(다른 Run)** 에서 시작해 SessionStart 복원이 뜨는지 함께 본다 (0.9.1 미확인 N6).

## 릴리스 완료 판정 (근거 없이 0 으로 적지 않는다)

```text
[x] PERSON_TYPED_SESSION_ISSUE_ON_THE_NORMAL_PATH = 0   (Q-2)
[x] PERSON_INVENTED_A_BODY_FOR_GIT_PUSH = 0
[x] PUBLISH_REFUSED_WHILE_STATUS_SAID_APPROVAL_OPEN = 0
[x] AUTHORITY_DERIVED_FROM_AN_OS_USER_NAME = 0
[x] BLOCKER_WITH_A_DIFFERENT_REMEDY_ON_TWO_SCREENS = 0   (Q-5·R-2)
[x] OWNERSHIP_RECOVERY_THAT_NEEDED_HOST_CLAUDE_OR_FORCE = 0   (R-2; 충돌 상황 조성에만 host claude bind 사용)
[x] TERMINAL_HOLDER_FORGOTTEN_WITHOUT_A_LOG_LINE = 0   (CLI 테스트; 실기계에서는 잔존 binding 이 이번 회차에 생기지 않음)
[x] EMPTY_BRANCH_READ_AS_MERGED = 0   (Q-6c)
[x] OFF_TARGET_MENTION_PRESENTED_AS_IMPLEMENTATION_EVIDENCE = 0   (Q-6b) — 반대 방향 오판(-649 ON-target 을 off 로) 1건은 0.10.1
[x] WORK_STATE_OVERRIDE_WITHOUT_A_HISTORY_ENTRY = 0
[x] PROVIDER_WORD_IN_CORE = 0   (B-29 gate)
[x] TAG_CUT_BEFORE_THE_ACCEPTANCE_RECORD = 0   (이 기록이 tag 보다 먼저 main 에 있다)
```

## 이 회차가 안고 가는 것

```text
liveness 미관측                          reclaim 은 근거만 보이고 산 Run 인지는 사람이 정한다. host 힌트는 이번 범위 밖
resource identity path-only · 결합 2개 거절  dogfood 가 닿지 않음. 실제 요구 뒤에
GitHub review() 부재                      GitHub publish 요구 0건
proposal 미보관                            닫힌 제안 파일이 쌓인다. 읽는 곳은 work issue 뿐
deprecated alias 3종                      두 minor 정책 도래를 또 미룸 — 주제 하나를 지키려고
Windows 실기계                             Q-7 로 시도. 못 하면 not taken
```

여섯 다 노트 Known limitations 에 적었다. 적었다는 것이 해결했다는 뜻이 아니다.

## 실행 기록 (2026-09-14/15, macOS, published 0.10.0)

publish run 34860616667 (dry run 34860371521 선행) → registry runtime 0.10.0 / bootstrap 0.10.0. acceptance 는 유지보수자
기계의 Claude Code Run 안에서 게시본 `asc update` 로 시작해 실 제품 workspace 에서 돌렸다. Q-2 가 본체이고 통과했다 —
자연어 요청에서 MR 까지 사람이 알아야 한 내부 primitive 0. Q-4 는 **FAIL** (`work issue` 가 canonical 을 읽는 통로 없이
조립돼 CANONICAL_UNAVAILABLE), Q-6 에서 병합된 작업 1건이 경로 표기 차이로 UNDECIDABLE — 둘 다 #91 로 고쳐 0.10.1 로
나간다. 이 기록은 0.10.0 게시본의 사실이며 finalize 는 그 사실 위에서 한다. Q-3·Q-7 not taken.
product acceptance: Q-1·Q-2·Q-5·R-2 PASS · Q-6 부분 · Q-4 FAIL(0.10.1) · Q-3·Q-7 not taken.
