# ASC v0.10.1 — 릴리스 체크리스트 (patch · 0.10.0 acceptance 가 찾은 둘)

정본 경로는 2단계다: `release.yml` (publish) 이후 `release-finalize.yml` (tag·Release).
finalize 는 이 파일의 `## 실행 기록` 섹션이 `main` 에 있어야 돈다.

## 이 릴리스가 싣는 것

```text
#91  asc work issue 가 scmFor(resolved) 로 조립된다 — canonical 선언 workspace 에서 CANONICAL_UNAVAILABLE 이던 자리 (0.10.0 Q-4 FAIL)
     언급 commit 경로 겹침에 경로 경계 꼬리 일치 — 패키지 상대 경로 vs 뿌리 경로 (0.10.0 Q-6a)
```

schema·identity·CLI 표면 변경 없음.

## Gate (publish 전)

```text
[x] npm run typecheck / build / test          — 1729 / 1728 pass / 0 fail / 1 skip
[x] npm run release:check                     — 0.10.1 명명·note 섹션·버전 리터럴
[x] npm run pack:all && npm run smoke                 — release.yml 안에서
[ ] npm run presentation:check                — 비게이트 (미실행)
[x] 3-OS CI green on main                     — #91 · #93
```

## 수동 fallback (워크플로 불능 시에만) — 이번 회차 미사용

```text
[ ] publish @asc-agent/runtime   → verify on the registry
[ ] publish @asc-agent/bootstrap → verify   (bootstrap 이 runtime 을 exact 로 가리킨다 — 순서 고정)
[ ] published smoke → 인수 → finalize
```

## Published acceptance (게시본으로만 · 실 workspace)

0.10.0 record 의 FAIL·부분 항목만 다시 본다.

```text
[x] P-1  asc update 0.10.0 → 0.10.1 · INSTALLED_CURRENT   PASS
[ ] P-2  (= 0.10.0 Q-4)   **FAIL (다른 원인)** — work start 가 거절된 제안(0.10.0 회차의 S-03)과 같은 id 로 초안을 내 저장이 조용히 실패, 화면은 "보존했다", work issue 는 "제안 없음".
         CANONICAL_UNAVAILABLE 자체는 재현되지 않음(발급 전에 막힘). #94 (usedIds 에 제안 id 포함 · 저장 실패를 저장했다고 말하지 않음) 로 0.10.2
[x] P-3  (= 0.10.0 Q-6a)   PASS — 같은 작업이 DECIDABLE_WITH_LIMITATION(leaning IMPLEMENTED_STALE_TRACKER, proxy)
[ ] P-4  위임 복원   보류 — 0.10.2 P-2 재실행 뒤 되돌린다
```

## 릴리스 완료 판정

```text
[ ] WORK_ISSUE_REFUSED_ON_A_CANONICAL_WORKSPACE = 0   — 실기계에서 확인 못 함 (P-2 가 발급 전에 막힘; CLI 테스트만)
[x] MERGED_WORK_LEFT_UNDECIDABLE_BY_PATH_PREFIX = 0   (P-3)
[x] TAG_CUT_BEFORE_THE_ACCEPTANCE_RECORD = 0
```

## 이 회차가 안고 가는 것

0.10.0 과 같다 (노트 Known limitations).

## 실행 기록 (2026-09-14/15, macOS, published 0.10.1)

publish run 34864384508 (dry 34864115877 선행) → registry 0.10.1/0.10.1. 실 제품 workspace 에서 P-1·P-3 PASS.
P-2 는 0.10.0 의 원인(CANONICAL_UNAVAILABLE)에 닿기 전에 **다른 결함**에 막혔다 — 제안 id 가 거절된 제안과 겹쳐
저장이 조용히 실패. #94 로 0.10.2. 이 기록은 0.10.1 게시본의 사실이며 finalize 는 그 위에서 한다.
