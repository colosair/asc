# ASC v0.10.2 — 릴리스 체크리스트 (patch · 0.10.1 acceptance 가 찾은 하나)

정본 경로는 2단계다: `release.yml` (publish) 이후 `release-finalize.yml` (tag·Release).
finalize 는 이 파일의 `## 실행 기록` 섹션이 `main` 에 있어야 돈다.

## 이 릴리스가 싣는 것

```text
#94  work start 의 새 세션 id 가 닫힌 제안의 id 를 다시 쓰지 않는다 · 저장 실패를 "보존했다" 고 말하지 않는다 (0.10.1 P-2)
```

## Gate (publish 전)

```text
[x] npm run typecheck / build / test          — 1731 / 1730 pass / 0 fail / 1 skip
[x] npm run release:check
[x] npm run pack:all && npm run smoke                 — release.yml 안에서
[ ] npm run presentation:check                — 비게이트 (미실행)
[x] 3-OS CI green on main                     — #94 · #96
```

## 수동 fallback (워크플로 불능 시에만) — 이번 회차 미사용

```text
[ ] publish @asc-agent/runtime → verify · publish @asc-agent/bootstrap → verify · published smoke → 인수 → finalize
```

## Published acceptance (게시본으로만 · 실 workspace)

```text
[x] P-1  asc update 0.10.1 → 0.10.2 · INSTALLED_CURRENT   PASS
[x] P-2  (= 0.10.0 Q-4 / 0.10.1 P-2)   PASS — work start → 제안 S-05 저장 (닫힌 S-03 을 건너뜀) · work status 에 보임 · `asc work issue S-05` → READY (발급 colosair · authority controller · recordedBy run · proposalId) · work start → CLAIMED · finish
[x] P-3  위임 복원 (implementer) · 재고정 · Blocked 없음   PASS
```

## 릴리스 완료 판정

```text
[x] PROPOSAL_REPORTED_SAVED_WHEN_IT_WAS_NOT = 0
[x] WORK_ISSUE_REFUSED_ON_A_CANONICAL_WORKSPACE = 0   (P-2 — canonical 선언 workspace 에서 발급)
[x] TAG_CUT_BEFORE_THE_ACCEPTANCE_RECORD = 0
```

## 이 회차가 안고 가는 것

0.10.0 과 같다.

## 실행 기록 (2026-09-15, macOS, published 0.10.2)

publish run 34867544316 (dry 34867257960 선행) → registry 0.10.2/0.10.2. 실 제품 workspace 에서 P-1·P-2·P-3 PASS.
0.10.0 이 열고 0.10.1·0.10.2 가 닫은 비위임 발급 경로가 이제 게시본에서 끝까지 간다: 제안 → `work issue` 한 줄 → CLAIMED.
외부 잔여물 없음 (관측용 세션 S-05 는 산출물 없이 DONE). 위임은 사람 결정값(implementer)으로 복원.
