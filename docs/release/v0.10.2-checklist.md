# ASC v0.10.2 — 릴리스 체크리스트 (patch · 0.10.1 acceptance 가 찾은 하나)

정본 경로는 2단계다: `release.yml` (publish) 이후 `release-finalize.yml` (tag·Release).
finalize 는 이 파일의 `## 실행 기록` 섹션이 `main` 에 있어야 돈다.

## 이 릴리스가 싣는 것

```text
#94  work start 의 새 세션 id 가 닫힌 제안의 id 를 다시 쓰지 않는다 · 저장 실패를 "보존했다" 고 말하지 않는다 (0.10.1 P-2)
```

## Gate (publish 전)

```text
[ ] npm run typecheck / build / test          — 1731 / 1730 pass / 0 fail / 1 skip
[ ] npm run release:check
[ ] npm run pack:all && npm run smoke
[ ] npm run presentation:check                — 비게이트
[ ] 3-OS CI green on main                     — #94 · release PR
```

## 수동 fallback (워크플로 불능 시에만)

```text
[ ] publish @asc-agent/runtime → verify · publish @asc-agent/bootstrap → verify · published smoke → 인수 → finalize
```

## Published acceptance (게시본으로만 · 실 workspace)

```text
[ ] P-1  asc update 0.10.1 → 0.10.2 · INSTALLED_CURRENT
[ ] P-2  (= 0.10.0 Q-4 / 0.10.1 P-2) 비위임: work start 가 새 id 로 proposal 저장 → `asc work issue S-…` → READY → work start CLAIMED → finish (산출물 없음)
[ ] P-3  위임을 사람이 정한 값(implementer)으로 되돌린다
```

## 릴리스 완료 판정

```text
[ ] PROPOSAL_REPORTED_SAVED_WHEN_IT_WAS_NOT = 0
[ ] WORK_ISSUE_REFUSED_ON_A_CANONICAL_WORKSPACE = 0
[ ] TAG_CUT_BEFORE_THE_ACCEPTANCE_RECORD = 0
```

## 이 회차가 안고 가는 것

0.10.0 과 같다.
